# Initialization
dictionary = dict()

# counter dict with 0 initialized
from collections import defaultdict
counter_dict = defaultdict(int)

# Existence of Key
if key in dictionary.keys():
  print(key + 'is present')
